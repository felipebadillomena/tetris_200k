{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "C/C++: g++ compilar proyecto (SDL2)",
            "type": "shell",
            "command": "g++ -fdiagnostics-color=always -g *.cpp -o main $(pkg-config --cflags --libs sdl2 SDL2_image)",
            "options": {
                "cwd": "${fileDirname}"
            },
            "problemMatcher": ["$gcc"],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}
